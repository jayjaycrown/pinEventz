import { ConpareValidatorDirective } from './conpare-validator.directive';

describe('ConpareValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ConpareValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
